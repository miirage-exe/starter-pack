{
  "name": "starter-pack",
  "version": "1.0.0",
  "description": "starter pack for node projects",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "sass-watch": "sass --watch public/sass:public/styles",
    "dev": "concurrently \"npm run sass-watch\" \"nodemon\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1",
    "ejs": "^3.1.8",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}
